apply plugin: 'maven'

group = 'com.github.vuduc4793'
version = '1.0'

uploadArchives {
    repositories {
        mavenDeployer {
            repository(url: "https://jitpack.io")
            pom.groupId = 'com.github.vuduc4793'
            pom.artifactId = 'Cloud-CA-Android-SDK'
            pom.version = '1.0'
        }
    }
}
